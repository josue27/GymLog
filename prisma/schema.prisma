generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id            String   @id @default(cuid())
  email         String   @unique
  passwordHash  String
  googleTokens  String?  // JSON encriptado con AES-256-GCM
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}
